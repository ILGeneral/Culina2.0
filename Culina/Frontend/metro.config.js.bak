const { getDefaultConfig } = require('expo/metro-config');

const config = getDefaultConfig(__dirname);

// Only disable .babelrc lookup
config.transformer.enableBabelRCLookup = false;

module.exports = config;